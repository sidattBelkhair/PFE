[server]
http_port = 3000
root_url = http://localhost:3000/

[security]
admin_user = admin
admin_password = SedadSOC2024!

[smtp]
enabled = true
host = smtp.gmail.com:587
user = ${GF_SMTP_USER}
password = ${GF_SMTP_PASSWORD}
from_address = ${GF_SMTP_USER}
from_name = SEDAD BANK SOC
startTLS_policy = MandatoryStartTLS

[alerting]
enabled = false

[unified_alerting]
enabled = true

[log]
mode = console
level = warn
