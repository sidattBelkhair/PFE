apiVersion: 1

providers:
  - name: SOC Dashboards
    orgId: 1
    folder: SOC
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
