import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Test basique — le vrai test nécessite un backend actif
    expect(true, isTrue);
  });
}
